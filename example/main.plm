type unit { unit }
type Action[A] {
  Print(A)
}

type Person {
  Person(int)
}

let x = Person(5)

fn biprint[A](x: A): Action[A] {
  Print(x);
  Print(x)
}

if x is Person(let age) then {
  biprint(x)
} else {
  Print(x)
}

